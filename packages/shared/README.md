# @gymlife/shared (optional)

Shared TypeScript types and constants for `apps/web` and future clients.

Add a `package.json` here when you want a workspace package; the root can use npm/pnpm workspaces later.
