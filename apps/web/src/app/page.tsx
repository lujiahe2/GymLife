export default function Home() {
  return (
    <main style={{ padding: "2rem", fontFamily: "system-ui" }}>
      <h1>GymLife</h1>
      <p>Next.js app — connect the agent API when ready.</p>
    </main>
  );
}
