# GymLife — Web (Next.js)

BFF for the UI: calls `services/api` for RAG/agent routes (add proxy or env `NEXT_PUBLIC_API_URL` later).

```bash
cd apps/web
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).
